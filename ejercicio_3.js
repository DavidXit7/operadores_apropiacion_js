//Aqui se declara las constantes
let contador;

//La constante es de 100
contador = 100;

//Se convierte la constante contador a float
contador = parseFloat(contador)

// Sumar 25 visitas adicionales
contador += 25;

// Restar 10 visitas por error de registro
contador -= 10;

// Duplicar el contador
contador *= 2;

// Mostrar el resultado final
console.log("Valor final del contador de visitas: ", contador);